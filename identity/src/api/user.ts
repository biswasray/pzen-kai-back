import express from "express";
import { IUserCreate } from "../interfaces/user";
export const userRouter = express.Router();

// userRouter.post(
//   "/",
//   (request: express.Request<unknown, unknown, IUserCreate>, response) => {

//   },
// );
